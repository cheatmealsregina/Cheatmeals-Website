const R = window.React;
export default R;
export const {
  useState, useEffect, useRef, useCallback, useMemo,
  useContext, createContext, memo, forwardRef, Fragment,
  Children, createElement, cloneElement, isValidElement,
  useId, useReducer, useLayoutEffect, useImperativeHandle,
  startTransition, Suspense, lazy,
} = R;
